{
    "buildCommand": "cd ../../packages/db && npm install && npx prisma generate && cd ../../apps/user-app && npm run build",
    "installCommand": "cd ../.. && npm install",
    "framework": "nextjs"
  }